name: data
description: A new Flutter package project.
version: 0.0.1
publish_to: none

environment:
  sdk: ">=2.17.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  dependencies:
    path: ../dependencies
  domain:
    path: ../domain
  flutter:
    sdk: flutter
  local:
    path: ../local
  network:
    path: ../network

dev_dependencies:
  build_runner: ^2.1.11
  flutter_lints: ^2.0.1
  flutter_test:
    sdk: flutter
  mocktail: ^0.3.0

flutter: