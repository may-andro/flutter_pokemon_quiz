name: ui_core
description: A new Flutter package project.
version: 0.0.1
publish_to: none

environment:
  sdk: ">=2.16.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  dependencies:
    path: ../../dependencies
  flutter:
    sdk: flutter

  # UI
  shimmer: ^2.0.0
  cached_network_image: ^3.2.0
  flutter_svg: ^1.0.3
  lottie: ^1.2.2
  fluttertoast: ^8.0.9
  provider: ^6.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter: