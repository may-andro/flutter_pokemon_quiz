import 'package:flutter/material.dart';

class IconErrorWidget extends StatelessWidget {
  const IconErrorWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
