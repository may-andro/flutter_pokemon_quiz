import 'package:flutter/material.dart';

class FullPageErrorWidget extends StatelessWidget {
  const FullPageErrorWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
