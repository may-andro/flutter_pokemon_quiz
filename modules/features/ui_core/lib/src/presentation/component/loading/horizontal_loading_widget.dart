import 'package:flutter/material.dart';

class HorizontalLoadingWidget extends StatelessWidget {
  const HorizontalLoadingWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
