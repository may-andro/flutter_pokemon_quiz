name: setting
description: A new Flutter package project.
version: 0.0.1
publish_to: none

environment:
  sdk: ">=2.16.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  ui_core:
    path: ../../features/ui_core
  dependencies:
    path: ../../dependencies
  domain:
    path: ../../domain
  flutter:
    sdk: flutter

dev_dependencies:
  build_runner: 2.1.5
  flutter_lints: ^1.0.4
  flutter_test:
    sdk: flutter

flutter:

scripts: ../../../derry.yaml