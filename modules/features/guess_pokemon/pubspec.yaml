name: guess_pokemon
description: A new Flutter package project.
version: 0.0.1
publish_to: none

environment:
  sdk: ">=2.17.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  ui_core:
    path: ../../features/ui_core
  dependencies:
    path: ../../dependencies
  domain:
    path: ../../domain
  flutter:
    sdk: flutter
  speech_to_text:
    git:
      url: https://github.com/SuaMusica/speech_to_text.git
      path: speech_to_text
      ref: fix/flutter3
  highlight_text: ^1.3.0

dev_dependencies:
  build_runner: ^2.1.11
  fake_async: ^1.3.0
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1
  flutter_gen_runner: ^4.2.0
  mocktail: ^0.3.0

flutter:
  assets:
    - assets/


flutter_gen:
  output: lib/assets/

scripts: ../../../derry.yaml