analyze:
  - cd modules/network && flutter analyze
  - cd modules/local && flutter analyze
  - cd modules/data && flutter analyze
  - cd modules/domain && flutter analyze
  - cd modules/features/ui_core && flutter analyze
  - cd modules/features/pokedex && flutter analyze
  - cd modules/features/guess_pokemon && flutter analyze
  - cd modules/features/pokemon_detail && flutter analyze
  - cd modules/features/setting && flutter analyze
  - flutter analyze
  - cd tools/pokedex_builder && flutter analyze

build_runner:
  - cd modules/network && flutter pub run build_runner build --delete-conflicting-outputs
  - cd modules/local && flutter pub run build_runner build --delete-conflicting-outputs
  - cd modules/data && flutter pub run build_runner build --delete-conflicting-outputs
  - cd modules/domain && flutter pub run build_runner build --delete-conflicting-outputs
  - cd modules/features/ui_core && flutter pub run build_runner build --delete-conflicting-outputs
  - cd modules/features/pokedex && flutter pub run build_runner build --delete-conflicting-outputs
  - cd modules/features/guess_pokemon && flutter pub run build_runner build --delete-conflicting-outputs
  - cd modules/features/pokemon_detail && flutter pub run build_runner build --delete-conflicting-outputs
  - cd modules/features/setting && flutter pub run build_runner build --delete-conflicting-outputs
  - flutter pub run build_runner build --delete-conflicting-outputs
  - cd tools/pokedex_builder && flutter pub run build_runner build --delete-conflicting-outputs

get_all:
  - flutter pub get
  - cd modules/dependencies && flutter pub get
  - cd modules/local && flutter pub get
  - cd modules/network && flutter pub get
  - cd modules/data && flutter pub get
  - cd modules/domain && flutter pub get
  - cd modules/features/ui_core && flutter pub get
  - cd modules/features/pokedex && flutter pub get
  - cd modules/features/guess_pokemon && flutter pub get
  - cd modules/features/pokemon_detail && flutter pub get
  - cd modules/features/setting && flutter pub get
  - cd tools/pokedex_builder && flutter pub get

run_jhoto:
  - flutter run lib/main_jhoto.dart --flavor jhoto

run_kanto:
  - flutter run lib/main_kanto.dart --flavor kanto

test:
  - cd modules/dependencies && flutter test
  - cd modules/local && flutter test
  - cd modules/network && flutter test
  - cd modules/data && flutter test
  - cd modules/domain && flutter test
  - cd modules/features/ui_core && flutter test
  - cd modules/features/pokedex && flutter test
  - cd modules/features/guess_pokemon && flutter test
  - cd modules/features/pokemon_detail && flutter test
  - cd modules/features/setting && flutter test
  - flutter test


  npm run rc validate kanto remote_config/kanto.json
  npm run rc publish kanto remote_config/kanto.json
  npm run rc get kanto remote_config/kanto.json
  npm run rc add-rc feature_toggle_pokedex true 'Controls the visibility of Pokedex page' kanto,johto
